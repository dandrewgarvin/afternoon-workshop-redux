const initialState = {
    isWorking: true
}

export default function Reducer(state = initialState, action){
    return state
}

export function addNumber (num){
    return {
        type,
        payload
    }
}